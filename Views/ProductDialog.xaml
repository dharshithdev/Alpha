<Window x:Class="Alpha.Views.ProductDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add/Edit Product"
        Height="550" Width="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="White">

    <Window.Resources>
        <!-- Reuse styles from ProductView -->
        <Style x:Key="FormTextBox" TargetType="TextBox">
            <Setter Property="Height" Value="36"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="BorderBrush" Value="#E2E8F0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">
                            <ScrollViewer x:Name="PART_ContentHost" 
                                          Margin="{TemplateBinding Padding}"
                                          VerticalContentAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsFocused" Value="True">
                    <Setter Property="BorderBrush" Value="#2C3E8A"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="FormNumericBox" TargetType="TextBox" BasedOn="{StaticResource FormTextBox}"/>

        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="#2C3E8A"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Height" Value="38"/>
            <Setter Property="Padding" Value="20,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#1E2F6F"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="CancelButton" TargetType="Button" BasedOn="{StaticResource PrimaryButton}">
            <Setter Property="Background" Value="#E2E8F0"/>
            <Setter Property="Foreground" Value="#4A5568"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#D0D5DD"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="Label">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#4A5568"/>
            <Setter Property="Margin" Value="0,0,0,5"/>
        </Style>
    </Window.Resources>

    <Grid Margin="25">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Title -->
        <TextBlock Grid.ColumnSpan="2" x:Name="TitleText" Text="Add New Product" 
                   FontSize="18" FontWeight="Bold" Foreground="#2D3748" Margin="0,0,0,20"/>

        <!-- Barcode -->
        <Label Grid.Row="1" Grid.Column="0" Content="Barcode" VerticalAlignment="Center" Margin="0,0,10,0"/>
        <TextBox Grid.Row="1" Grid.Column="1" x:Name="BarcodeBox" Style="{StaticResource FormTextBox}"
                 ToolTip="Scan barcode or enter manually"/>

        <!-- Name -->
        <Label Grid.Row="2" Grid.Column="0" Content="Product Name *" VerticalAlignment="Center" Margin="0,0,10,5"/>
        <TextBox Grid.Row="2" Grid.Column="1" x:Name="NameBox" Style="{StaticResource FormTextBox}"
                 ToolTip="Enter product name (required)"/>

        <!-- Category -->
        <Label Grid.Row="3" Grid.Column="0" Content="Category" VerticalAlignment="Center" Margin="0,0,10,5"/>
        <TextBox Grid.Row="3" Grid.Column="1" x:Name="CategoryBox" Style="{StaticResource FormTextBox}"
                 ToolTip="Product category (e.g., Electronics, Food)"/>

        <!-- Description -->
        <Label Grid.Row="4" Grid.Column="0" Content="Description" VerticalAlignment="Top" Margin="0,0,10,5"/>
        <TextBox Grid.Row="4" Grid.Column="1" x:Name="DescriptionBox" Style="{StaticResource FormTextBox}" 
                 Height="60" TextWrapping="Wrap" AcceptsReturn="True"
                 ToolTip="Product description (optional)"/>

        <!-- Purchase Price -->
        <Label Grid.Row="5" Grid.Column="0" Content="Purchase Price" VerticalAlignment="Center" Margin="0,0,10,5"/>
        <TextBox Grid.Row="5" Grid.Column="1" x:Name="PurchasePriceBox" Style="{StaticResource FormTextBox}"
                 ToolTip="Cost price (how much you paid)"/>

        <!-- Selling Price -->
        <Label Grid.Row="6" Grid.Column="0" Content="Selling Price *" VerticalAlignment="Center" Margin="0,0,10,5"/>
        <TextBox Grid.Row="6" Grid.Column="1" x:Name="SellingPriceBox" Style="{StaticResource FormTextBox}"
                 ToolTip="Selling price (required)"/>

        <!-- Quantity -->
        <Label Grid.Row="7" Grid.Column="0" Content="Quantity *" VerticalAlignment="Center" Margin="0,0,10,5"/>
        <TextBox Grid.Row="7" Grid.Column="1" x:Name="QuantityBox" Style="{StaticResource FormTextBox}" Text="0"
                 ToolTip="Current stock quantity (required)"/>

        <!-- Min Stock -->
        <Label Grid.Row="8" Grid.Column="0" Content="Min Stock Alert" VerticalAlignment="Center" Margin="0,0,10,5"/>
        <TextBox Grid.Row="8" Grid.Column="1" x:Name="MinStockBox" Style="{StaticResource FormTextBox}" Text="10"
                 ToolTip="Low stock alert level (default: 10)"/>

        <!-- Error Message -->
        <TextBlock Grid.Row="9" Grid.ColumnSpan="2" x:Name="ErrorText" Foreground="#E53E3E" 
                   FontSize="12" Margin="0,10,0,10" Visibility="Collapsed"/>

        <!-- Buttons -->
        <StackPanel Grid.Row="10" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button x:Name="SaveBtn" Content="💾 Save" Style="{StaticResource PrimaryButton}" Click="SaveBtn_Click" Margin="0,0,10,0"/>
            <Button Content="✕ Cancel" Style="{StaticResource CancelButton}" Click="CancelBtn_Click"/>
        </StackPanel>
    </Grid>
</Window>